# 未决问题（Open Questions）

> 讨论收敛后将条目移至 `decisions/ADR-*.md` 并关闭。

## 高优先级（影响 MVP）

| ID | 问题 | 选项 | 倾向 | 状态 |
|----|------|------|------|------|
| OQ-01 | MVP 是否纯前端（AI 经 serverless 代理）？ | A 纯前端+代理 / B 全栈 | A | 待决 |
| OQ-02 | `@DIALOGUE` 进行中能否与其他 actor 的 `@MOVE_TO` 并行？ | 允许 / 禁止 | 允许（不同 actor） | 待决 |
| OQ-03 | 用户点击跳过对话时，是否压缩 `duration_estimate`？ | 是 / 否 | 否（计时按剧本） | 待决 |
| OQ-04 | `lily` 是否纳入首个 AI 生成剧本的默认 cast？ | 是 / 否 | 否（MVP 2 人戏） | 待决 |

## 中优先级（影响体验）

| ID | 问题 | 选项 | 状态 |
|----|------|------|------|
| OQ-05 | 是否需要 beat 时间（音乐节拍）除秒以外？ | 仅秒 / 增加 beat | 待决 |
| OQ-06 | 斜向移动是否在下个版本开放？ | 是 / 否 | 待决 |
| OQ-07 | 道具 `letter` 是否允许 `@SPAWN_PROP` 放地面？ | 仅 GIVE / 可放置 | 待决 |
| OQ-08 | 回放是否需要快进/跳转 `@ACT`？ | 是 / 否 | 待决 |
| OQ-09 | AI 输出语言：仅中文还是多语言 front matter？ | zh-CN / i18n | 待决 |

## 低优先级（后置）

| ID | 问题 | 状态 |
|----|------|------|
| OQ-10 | 可视化剧本编辑器是否独立于 DSL 文本？ | 待决 |
| OQ-11 | 用户自定义角色是否上传 sprite？ | 待决 |
| OQ-12 | 是否需要剧本评分/点赞？ | 待决 |
| OQ-13 | 语音合成与口型同步方案？ | 待决 |

## 已关闭

（暂无）

---

### 如何关闭一条问题

1. 在 [`decisions/`](./decisions/) 新建 `ADR-NNN-简短标题.md`
2. 记录：背景、决策、后果
3. 更新相关 L0–L7 文档
4. 将本表中对应行移至「已关闭」
